# MiniGit
Here I have tried to implement Basic Git Functions like add, commit, hashing(using hashlib built in sha1 function), commit parser using Python.
Also I have implemented a head pointer to keep track of latest commits
